[project]
name = "agent-gemini"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colorama>=0.4.6",
    "google-genai>=1.60.0",
    "pyfiglet>=1.0.4",
    "termcolor>=3.3.0",
]
[project.scripts]
agent = "main:main"

[tool.setuptools]
py-modules = ["main", "tools"]
